function createElement(data) {
  // create paragraph with data as the text
  const pp = document.createElement("p");
  pp.textContent = data;
  document.body.appendChild(pp);
}

function queryWikipedia(callback) {

}
